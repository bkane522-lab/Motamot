{
  "name": "motamot",
  "version": "1.0.0",
  "private": true,
  "description": "PWA de traduction de textes — fonctions serverless Vercel",
  "dependencies": {
    "pdf-parse": "^1.1.1"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
