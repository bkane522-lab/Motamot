{
  "name": "motamot",
  "version": "1.0.0",
  "private": true,
  "description": "PWA de traduction de textes — fonctions serverless Vercel",
  "dependencies": {
    "unpdf": "^0.11.0"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
